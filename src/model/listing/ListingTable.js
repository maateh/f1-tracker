class ListingTable {
  constructor({
    columns, 
    data, 
    pagination
  }) {
    this.columns = columns
    this.data = data
    this.pagination = pagination
  }
}

export default ListingTable

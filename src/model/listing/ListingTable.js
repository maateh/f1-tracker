class ListingTable {
  constructor({
    columns, 
    data, 
    pageQuantity
  }) {
    this.columns = columns
    this.data = data
    this.pageQuantity = pageQuantity
  }
}

export default ListingTable
